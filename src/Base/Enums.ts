
export enum urls {
    login = '/login',
    home = '/home',
    dashboard = '/dashboard',
    register = '/register',
}
export enum status { 
    success = 200,
    update = 201,
    resource_not_found = 404,
    bad_request = 400,
}